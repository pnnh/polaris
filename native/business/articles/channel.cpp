
#include "channel.h"

std::shared_ptr<std::vector<polaris::models::articles::PSChannelModel> > polaris::native::business::ChannelBusiness::
ChannelServerBusiness::selectChannels() {
    auto channels = std::make_shared<std::vector<polaris::models::articles::PSChannelModel> >();


    return channels;
}
