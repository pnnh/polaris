import {SessionModel} from '@/models/session'

export function getLoginSession() {
    // const url = '/server/account/session'
    // const response = await fetch(url) as SessionModel
    // return response
    return {} as SessionModel
}

